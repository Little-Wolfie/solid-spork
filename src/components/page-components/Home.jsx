import ArticleContainer from '../home-components/ArticleContainer';
import Sort from '../home-components/Sort';

const Home = () => {
	return (
		<main>
			<Sort />
			<ArticleContainer />
		</main>
	);
};

export default Home;
